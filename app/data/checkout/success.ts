// ─── Constants ──────────────────────────────────────

/** Maximum number of polling attempts before giving up. */
export const MAX_POLL_ATTEMPTS = 15;

/** Interval between each poll in milliseconds. */
export const POLL_INTERVAL_MS = 2000;

/** Default message shown while polling for subscription confirmation. */
export const DEFAULT_POLL_MESSAGE = "Confirming your subscription...";
